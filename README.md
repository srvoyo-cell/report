# ReportAI

**ReportAI** — инструмент для автоматической генерации университетских отчётов (Markdown → DOCX) на основе теоретических материалов, исходного кода и LLM-модели (через HuggingFace Router / OpenAI‑compatible API).

Проект предназначен для автоматизации подготовки лабораторных и курсовых отчётов: восстановление теории, генерация раздела **«Ход работы»**, построение графиков и сборка финального DOCX-документа, готового к сдаче.

---

## 🚀 Возможности

* 📄 Загрузка теории из `theory.docx`
* 🧠 Восстановление математических формул и структуры текста через LLM
* ✍️ Генерация раздела **«Ход работы»** в формате Markdown
* 📊 Автоматическое построение графиков на основе кода, возвращённого моделью
* 🔄 Конвертация Markdown → DOCX с помощью Pandoc
* 🧾 Поддержка reference DOCX-шаблона (оформление под требования ВУЗа)
* 🪵 Подробное логирование всех этапов выполнения

---

## 📁 Структура проекта

```
report/
├── for_reports/              # Входные данные
│   ├── theory.docx           # Теоретическая часть
│   ├── *.py / *.cpp / *.R    # Исходный код лабораторной работы
│
├── for_reports/output/       # Результаты генерации
│   ├── report.md             # Сгенерированный Markdown-отчёт
│   ├── report.docx           # Финальный DOCX-отчёт
│   ├── resp.txt              # JSON с кодом для построения графиков
│   ├── *.png                 # Сохранённые графики
│
├── help_functions.py         # Декораторы и вспомогательные функции
├── templates.py              # Jinja2-шаблоны промптов для LLM
├── reportai_class.py         # Основной класс ReportAI
├── main.py                   # Точка входа
├── requirements.txt          # Зависимости
└── .gitignore
```

---

## ⚙️ Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/<username>/report.git
cd report
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Установка Pandoc

Ubuntu / Debian:

```bash
sudo apt install pandoc
```

macOS:

```bash
brew install pandoc
```

Windows: установить с официального сайта Pandoc.

---

## 🔑 Переменные окружения

Создай файл `.env` в корне проекта:

```env
HF_TOKEN=your_huggingface_token
```

Токен используется для доступа к модели через HuggingFace Router.

---

## ▶️ Использование

1. Помести входные данные в папку `for_reports/`:

   * `theory.docx` — теоретическая часть
   * файлы с кодом (`.py`, `.cpp`, `.R`)

2. Запусти генерацию отчёта:

```bash
python main.py
```

3. Результат появится в папке:

```
for_reports/output/
```

* `report.md` — промежуточный Markdown
* `report.docx` — финальный отчёт
* `*.png` — сгенерированные графики

---

## 🧠 Как это работает (кратко)

1. Загружается теория и исходный код
2. LLM восстанавливает формулы и структуру теории
3. Генерируется раздел **«Ход работы»**
4. Модель возвращает JSON с кодом для графиков
5. Код исполняется локально, графики сохраняются
6. Markdown конвертируется в DOCX через Pandoc

---

## ⚠️ Важно

* Для генерации графиков модель **обязана** вернуть корректный JSON
* Код графиков выполняется локально — не используй непроверенные модели
* Формулы оформляются в `$...$` / `$$...$$` для корректной обработки Pandoc

---

## 📌 Назначение проекта

Проект ориентирован на:

* студентов технических специальностей
* автоматизацию лабораторных и курсовых работ
* быстрое и стандартизированное оформление отчётов

---

## 🧑‍💻 Автор

Разработано для автоматизации учебных отчётов и экспериментов с LLM.

Если ты используешь это для ВУЗа — **проверяй результат перед сдачей**.
